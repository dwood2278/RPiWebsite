<!DOCTYPE html>
<html>
    <head>
        <% include ../partials/head %>
    </head>
    <body>
        <div id="app" v-cloak>
            <header>
                <% include ../partials/header %>
            </header>
            <main>
                <div class="container">
                    <% if (!locals.userChangeSuccess) { %>
                        <div class="row">
                            <div class="col-12">
                                <edit-user
                                    :user = '<%- JSON.stringify(locals.user) %>'
                                ></edit-user>
                            </div>
                        </div>
                    <% } else { %>
                        <div class="row">
                            <div class="col-12">
                                <div class="alert alert-success" role="alert">
                                    <i class="fas fa-check"></i> Profile updated.
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-12">
                                <a href="/users/editUser">Update your profile again.</a>
                            </div>
                        </div>
                    <% } %>
                </div> 
            </main>
            <footer class="border-top footer text-muted bg-dark">
                <% include ../partials/footer %>
            </footer>
        </div>
        <script src="/build/bundle.js"></script>
    </body>
</html> 